services:
  mikupad:
    build:
      context: ..
      dockerfile: server/Dockerfile
    ports:
      - 2910:3000
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./storage:/storage
    extra_hosts:
      - host.docker.internal:host-gateway

volumes:
  storage:
